spring:
  application:
    name: banks-shop

  datasource:
    url: jdbc:mariadb://localhost:3306/banks_shop_db
    username: root
    password: Olotu1234_
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
